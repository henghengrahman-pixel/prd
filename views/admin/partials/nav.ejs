<aside class="admin-sidebar">
  <h2>Dashboard</h2>
  <a href="<%= baseUrl %>/<%= adminPath %>/dashboard">Overview</a>
  <a href="<%= baseUrl %>/<%= adminPath %>/settings">Settings</a>
  <a href="<%= baseUrl %>/<%= adminPath %>/posts">Posts</a>
  <a href="<%= baseUrl %>/<%= adminPath %>/categories">Categories</a>
  <a href="<%= baseUrl %>/<%= adminPath %>/pages">Pages</a>
  <a href="<%= baseUrl %>/<%= adminPath %>/media">Media</a>
  <form method="post" action="<%= baseUrl %>/<%= adminPath %>/logout">
    <button type="submit" class="logout-btn">Logout</button>
  </form>
</aside>
