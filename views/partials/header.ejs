<header>
  <div id="top-header">
    <div class="container">
      <div class="top-header-wrap">
        <ul class="header-links header-links-left">
          <% if (settings.phone) { %>
            <li>
              <a href="tel:<%= settings.phone %>">
                <i class="fa fa-phone"></i>
                <span><%= settings.phone %></span>
              </a>
            </li>
          <% } %>

          <% if (settings.email) { %>
            <li>
              <a href="mailto:<%= settings.email %>">
                <i class="fa fa-envelope-o"></i>
                <span><%= settings.email %></span>
              </a>
            </li>
          <% } %>
        </ul>

        <ul class="header-links header-links-right">
          <% if (settings.whatsapp) { %>
            <li>
              <a href="https://wa.me/<%= String(settings.whatsapp).replace(/\D/g, '') %>" target="_blank">
                <i class="fa fa-whatsapp"></i>
                <span>WhatsApp</span>
              </a>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </div>

  <div id="header">
    <div class="container">
      <div class="header-wrap">

        <!-- LOGO -->
        <div class="header-logo">
          <a href="/" class="logo">
            <img src="<%= settings.logo %>" alt="<%= settings.storeName %>">
          </a>
        </div>

        <!-- SEARCH -->
        <div class="header-search">
          <form method="get" action="/shop" class="search-form">
            <input
              class="input"
              type="text"
              name="q"
              value="<%= typeof query !== 'undefined' ? query : '' %>"
              placeholder="Cari produk..."
            >
            <button class="search-btn" type="submit">Cari</button>
          </form>
        </div>

        <!-- CART + MENU -->
        <div class="header-ctn">
          <div class="cart-area">
            <a href="/cart" class="cart-toggle">
              <i class="fa fa-shopping-cart"></i>
              <span>Keranjang</span>
              <div class="qty"><%= cartCount %></div>
            </a>
          </div>

<div class="menu-toggle">
  <a href="javascript:void(0)" id="mobile-menu-toggle" aria-label="Buka menu">
    <i class="fa fa-bars"></i>
    <span>Menu</span>
  </a>
</div>

      </div>
    </div>
  </div>
</header>

<nav id="navigation">
  <div class="container">
    <div id="responsive-nav"><div id="responsive-nav">
  <div class="mobile-nav-header">
    <span>Menu</span>
    <button type="button" id="mobile-menu-close" class="mobile-menu-close" aria-label="Tutup menu">
      <i class="fa fa-times"></i>
    </button>
  </div>
  <ul class="main-nav nav navbar-nav">
        <li class="<%= currentPath === '/' ? 'active' : '' %>"><a href="/">Home</a></li>
        <li class="<%= currentPath.startsWith('/shop') ? 'active' : '' %>"><a href="/shop">Shop</a></li>
        <li class="<%= currentPath.startsWith('/articles') ? 'active' : '' %>"><a href="/articles">Artikel</a></li>
        <li class="<%= currentPath.startsWith('/contact') ? 'active' : '' %>"><a href="/contact">Kontak</a></li>
        <li class="<%= currentPath.startsWith('/cart') ? 'active' : '' %>"><a href="/cart">Cart</a></li>
      </ul>
    </div>
  </div>
</nav>

<div class="section flash-section">
  <div class="container">
    <% if (flash) { %>
      <div class="alert alert-<%= flash.type %> flash-alert"><%= flash.message %></div>
    <% } %>
  </div>
</div>
